# ============================================
# Sentry AI Environment Configuration
# ============================================
# Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin:
#   cp .env.example .env
#
# ⚠️ .env dosyasını versiyon kontrolüne EKLEMEYİN!

# ── PostgreSQL ────────────────────────────────
POSTGRES_DB=sentry_db
POSTGRES_USER=sentry
POSTGRES_PASSWORD=camcam22xy

# ── Java Backend (Spring Boot) ────────────────
DB_URL=jdbc:postgresql://postgres:5432/sentry_db
DB_USERNAME=sentry
DB_PASSWORD=camcam22xy

# ── JWT Secret ────────────────────────────────
# Minimum 256-bit (32 byte), Base64 encoded olmalı.
# Oluşturmak için:
#   Linux/Mac:  openssl rand -base64 32
#   PowerShell: [Convert]::ToBase64String((1..32 | % { Get-Random -Max 256 }) -as [byte[]])
JWT_SECRET=

# ── AI Engine ↔ Management Server ────────────
# Servisler arası iletişim için API key.
# Oluşturmak için:
#   Linux/Mac:  openssl rand -hex 32
#   PowerShell: -join ((1..64) | % { '{0:x}' -f (Get-Random -Max 16) })
AI_API_KEY=

# ── Python AI Engine ──────────────────────────
BACKEND_URL=http://management-server:8080
DEBUG=false
MODEL_PATH=yolov8n.pt
DETECTION_CONFIDENCE=0.6
AUDIO_THRESHOLD=0.7
